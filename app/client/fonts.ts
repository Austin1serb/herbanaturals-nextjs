import { Poppins, Lusitana } from 'next/font/google';

export const poppins = Poppins({ weight: ['200', '400'], subsets: ['latin'], display: 'swap' });
export const lusitana = Lusitana({ weight: ['400', '700'], display: 'swap', subsets: ['latin'] });
